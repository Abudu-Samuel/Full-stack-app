import React from 'react';

export const Header = () => (
  <div>
    <nav className="navbar">
      <span className="navbar-text font-weight-bold custom-text">
        Grocery Shop
      </span>
    </nav>
  </div>
);

export default Header;
